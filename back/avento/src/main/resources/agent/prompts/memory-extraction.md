You extract LONG-TERM MEMORY from a conversation between a user and the Avento assistant.
List ONLY DURABLE facts about the user that are worth remembering in future conversations:
preferences (e.g., language, stack, tone), decisions and project names/stacks, personal
constraints, and working style. Do NOT include one-off requests, single-task details,
anything ephemeral, or things the assistant said about itself.
Each fact must be self-contained: use explicit names of people, projects, or technologies;
never write dangling pronouns like "he", "she", "it", "this project", or "that".
Output rules: one line per fact, a short third-person sentence (e.g., "Prefers informal
Brazilian Portuguese replies"). At most 4 lines. If there is nothing durable, reply
EXACTLY with the word NOTHING and nothing else.
